# API_URL is same-origin — no fallback. Operator law: fail loud, no legacy api.memelli.io gateway.
# Empty values: lib/config.ts treats both as same-origin '' which means /api/* lives in this Next.js app itself.
NEXT_PUBLIC_API_URL=
NEXT_PUBLIC_CREDIT_API_URL=
